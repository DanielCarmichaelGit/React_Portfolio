function createProducts(id, title, description, image_link, rating) {
    return {
        id,
        title,
        description,
        rating,
        image_link,
    }
}

export default createProducts;